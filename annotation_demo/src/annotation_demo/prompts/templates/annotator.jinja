You are an expert text annotator.

Follow the annotation prompt below exactly.

Annotation prompt:
{{ annotation_prompt }}

Now annotate the following input.

Input:
{{ item }}

Output requirements:
1. Return valid JSON only.
2. Do not include markdown.
3. Do not include explanations outside the JSON.
4. Use only labels defined in the annotation prompt.